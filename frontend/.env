# Copy to .env in this folder and set YOUR Mac Wi‑Fi IP (same network as your phone).
# Do not use localhost — on a physical phone, localhost is the phone itself.
#
#   cp .env.example .env
#   # Edit .env and replace 192.168.x.x with your IP from: ipconfig getifaddr en0
#
EXPO_PUBLIC_API_BASE_URL=http://192.168.29.35:5001
