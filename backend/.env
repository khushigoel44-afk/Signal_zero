PORT=5001
MONGODB_URI=mongodb://127.0.0.1:27017/ogtps
ML_SERVICE_URL=http://localhost:8000
# Omit or use * for dev (allows Expo / any origin). For production, set comma-separated URLs.
# CORS_ORIGIN=http://localhost:8081,http://192.168.1.100:8081
CORS_ORIGIN=
JWT_SECRET=replace-with-strong-secret
